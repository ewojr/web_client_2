# Dev-server proxy target — where `npm run dev` forwards the PD Server routes
# (/v2.0, /file, /temp). Point this at your Password Depot Server.
#
# Copy this file to `.env.local` (gitignored) and edit. Defaults to
# https://localhost:8714 if unset.
DEV_PROXY_TARGET=https://localhost:8714
